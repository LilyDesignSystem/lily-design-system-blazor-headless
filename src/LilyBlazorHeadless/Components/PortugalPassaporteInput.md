# PortugalPassaporteInput

A an input for entering Portugal's Passaporte.

See `components/portugal-passaporte-input/index.md` for canonical documentation.

## Parameters

- `Label`: string (required) — accessible label set on `aria-label`
- `CssClass`: string — extra CSS classes appended to `portugal-passaporte-input`

- `AdditionalAttributes`: catches unmatched HTML attributes

## Usage

```razor
<PortugalPassaporteInput Label="Portuguese Passport" />
```
