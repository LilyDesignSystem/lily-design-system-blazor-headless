# RomaniaCodNumericPersonalInput

A an input for entering Romania's Cod Numeric Personal (CNP).

See `components/romania-cod-numeric-personal-input/index.md` for canonical documentation.

## Parameters

- `Label`: string (required) — accessible label set on `aria-label`
- `CssClass`: string — extra CSS classes appended to `romania-cod-numeric-personal-input`

- `AdditionalAttributes`: catches unmatched HTML attributes

## Usage

```razor
<RomaniaCodNumericPersonalInput Label="Personal Numerical Code" />
```
