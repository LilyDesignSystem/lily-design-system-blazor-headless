# PolskaPeselInput

A an input for entering Poland's PESEL.

See `components/polska-pesel-input/index.md` for canonical documentation.

## Parameters

- `Label`: string (required) — accessible label set on `aria-label`
- `CssClass`: string — extra CSS classes appended to `polska-pesel-input`

- `AdditionalAttributes`: catches unmatched HTML attributes

## Usage

```razor
<PolskaPeselInput Label="PESEL" />
```
